
<%= form_for(@lieu) do |f| %>
  <% if @lieu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lieu.errors.count, "error") %> prohibited this lieu from being saved:</h2>

      <ul>
      <% @lieu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label "Lieu" %><br>
    <%= f.text_field :nom, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label "Partagez vos impressions" %><br>
    <%= f.text_area :commentaire, class: 'form-control input-lg' %>
  </div>
  <div class="form-group">
    <%= f.label "Donnez une note" %><br>
    <%= f.number_field :note, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label "Le recommandez-vous aux autres ? " %><br>
    <%= f.check_box :recommandation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
